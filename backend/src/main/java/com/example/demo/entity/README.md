# Entity

Esta carpeta contendra las entidades JPA que representan la persistencia.

Ejemplos:

- Clases anotadas con `@Entity`.
- Identificadores con `@Id` y `@GeneratedValue`.
- Relaciones entre tablas cuando el modelo las necesite.

Las entidades se crean despues de aprobar el modelo de datos y el contrato.
