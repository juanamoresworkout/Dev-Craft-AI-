# Services

Esta carpeta contendra los servicios Angular reutilizables que llamen a la API
Spring Boot mediante `HttpClient`.

Usar rutas relativas como `/api/tareas` para aprovechar `proxy.conf.json`
durante el desarrollo.
